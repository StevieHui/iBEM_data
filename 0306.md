- 坐标原始数据不用弄了；

- 原点和坐标轴写在readme；

- 底图readme里边；

- 检查xy变换、前两圈可以拿掉；

- raw_csv改成正常的一圈坐标； 

- 做出一组场图，放到readme；同时检查npz对不对；（我们这里用的是纯空间插值，RBF，仅作为示例，用户可以自由选择

- 自动化调用的cfd模拟工具 支持要素快速定义/快速模拟（不用fluent等用openFOAM）的cfd仿真工具   能耗仿真和室内物体人员活动关系没那么大

- 检索现有研究直接拿来能用的室内建筑环境黑箱模型，机器人同步采的可以作为ground truth

- 场重建的图整仨  图一温度在不同时刻的变化   图二找一个变化明显的参数的时间变化  展示同一时刻不同参数的分布特征

- 插值后的矩阵 通过空间关联 对矩阵进行分块 和对应区域图片绑定在一起 热成像也可以一块

- 鸿雁要结项

- 标注：识别出物体以及和机器人的位置关系坐标位置变换

- 再丢点数据进去
